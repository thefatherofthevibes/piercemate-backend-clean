{
  "phases": {
    "install": {
      "cmds": ["npm install --legacy-peer-deps"]
    },
    "build": {
      "cmds": ["NODE_ENV=production npm run build"]
    }
  },
  "start": {
    "cmd": "medusa db:migrate && medusa db:sync-links && medusa start"
  }
}
