{
  "phases": {
    "install": {
      "cmds": ["npm install --legacy-peer-deps"]
    }
  },
  "start": {
    "cmd": "npm run build && medusa db:migrate && medusa db:sync-links && medusa start"
  }
}
